name: workflow
on: 
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:
    inputs:
      environment:
        description: "In which environment you want to run (test,beta)"
        required: true
        default: "test"  

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "Hello g"
